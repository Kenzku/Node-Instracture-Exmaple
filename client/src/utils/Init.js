/**
 * Created by Huang, Fuguo (a.k.a Ken) on 16/08/2017.
 */
export function init(done = () => {}) {
  let initCondition = null; /*you can assume from url, cookies, session storage or somewhere else*/

  /*try websocket from server*/
  if(!initCondition) {

  }
  done(initCondition);
}
